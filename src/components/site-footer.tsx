export function SiteFooter() {
  return (
    <footer className="mt-auto border-t">
      <div className="mx-auto max-w-5xl px-4 py-6 text-center text-xs text-muted-foreground sm:px-6">
        Demo environment — simulated eligibility data, one live registry lookup (NPPES), and
        manually ingested coverage policies. Nothing here is a coverage determination.
      </div>
    </footer>
  );
}
